CAPI=2:
name: "aidan:APB_UART:apb_uart_rx:1.0.0"
description: ""

filesets:
    rtl:
        files:
            - "source/apb_uart_rx.sv"
        file_type: systemVerilogSource
        depend:
            - "aidan:APB_UART:rcv_block:1.0.0"
            - "aidan:APB_UART:apb_subordinate:1.0.0"

    synth:
        depend:
            - "APB_UART:tech:AMI_05_LIB"
            - "aidan:APB_UART:apb_uart_rx_syn"

    tb:
        files:
            - "testbench/tb_apb_uart_rx.sv"
            - "waves/apb_uart_rx.do": { file_type: user }
            - "waves/apb_uart_rx.gtkw": { file_type: user }
        file_type: systemVerilogSource
        depend:
            - "aidan:APB_UART:apb_model"

    synfiles:
        depend:
            - "aidan:APB_UART:synfiles"

targets:
    default: &default
        filesets:
            - rtl
        toplevel: apb_uart_rx

    sim: &sim
        <<: *default
        default_tool: verilator
        filesets_append:
            - tb
        toplevel: tb_apb_uart_rx
        tools:
            modelsim:
                vsim_options:
                    - -vopt
                    - -voptargs='+acc'
                    - -t ps
                    - -do waves/apb_uart_rx.do
                    - -onfinish stop
                    - -do "set PrefSource(OpenOnFinish) 0 ; set PrefMain(LinePrefix) \"\" ; set PrefMain(colorizeTranscript) 1"
                    - -coverage
                vlog_options:
                    - +cover
            verilator:
                verilator_options:
                    - --cc
                    - --trace
                    - --main
                    - --timing
                    - --coverage
                make_options:
                    - -j

    lint:
        <<: *default
        default_tool: verilator
        filesets_append:
            - tb
        tools:
            verilator:
                mode: lint-only
                verilator_options:
                    - --timing
                    - -Wall

    syn:
        <<: *default
        filesets_append:
            - synfiles
        default_tool: design_compiler
        toplevel: apb_uart_rx
        tools:
            design_compiler:
                script_dir: "src/u337mg054_ece337_synfiles_0"
                dc_script: "synth.tcl"
                report_dir: "reports"
                target_library: "/home/ecegrid/a/ece337/summer24-refactor/tech/ami05/osu05_stdcells.db"
                libs: "/home/ecegrid/a/ece337/summer24-refactor/tech/ami05/osu05_stdcells.db dw_foundation.sldb"

    syn_sim:
        <<: *sim
        filesets:
            - synth
            - tb

